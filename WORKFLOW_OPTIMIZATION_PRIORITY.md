# 论文阅读工作流优化优先级

版本：v0.1.1
维护对象：Codex 论文阅读工作流
用途：把 `SESSION_REVIEW-*.md` 中的观察转化为经过判断的优化任务，并按照本文件从上到下的顺序迭代。

## 使用原则

1. `SESSION_REVIEW` 是证据和建议来源，不是自动变更清单。
2. 先理解问题、影响范围和根因，再决定是否进入优化队列。
3. 只有具有通用性、可复现性和明确收益的问题，才修改工作流。
4. 已被现有规则覆盖的问题标记为“无需改动”，不重复增加规则。
5. 低优先级任务不得抢在高优先级任务之前实施。
6. 每次优化都记录来源 review、判断理由、改动范围和验证结果。
7. 与单篇论文内容有关、但不能泛化为工作流问题的建议，保留在该论文 review 中，不强行写入 Skill。

## 优先级定义

- **P0**：会导致错误保存、错误归因、用户修改被覆盖或核心阅读目标偏移的问题。
- **P1**：显著影响阅读质量、证据可追溯性或跨会话复用的问题。
- **P2**：改善体验、验证或可维护性，但当前没有明确失败证据的问题。
- **P3**：便利性、风格或未来扩展，不阻塞当前工作流。

## 当前队列

| 顺序 | 优先级 | 任务 | 来源 | 判断 | 状态 |
|---:|:---:|---|---|---|---|
| 1 | P0 | 建立 SESSION_REVIEW → 理性评估 → 优先级迭代闭环 | `SESSION_REVIEW-2026-07-27-DeepSeekMath.md` §1、§6、§8 | 这是用户明确要求的治理机制，必须先建立 | 已实施 |
| 2 | P0 | 阅读开始时锁定用户重点，并据此分配章节深度 | review §3.1、§3.5 | 会直接影响阅读时间和核心产出，具有通用性 | 已实施 |
| 3 | P1 | 主动提取论文作者提供的统一框架、分类法和高层变量分解 | review §3.2 | 能避免只做局部摘要，适用于方法型论文 | 已实施 |
| 4 | P1 | 强化公式、编码、YAML 路径和外部写入的保存安全规则 | review §4.3–§4.6 | 已有真实失败记录，且可通过规则降低风险 | 已实施 |
| 5 | P1 | 用户已有笔记的后续修订必须先读当前文件并做局部补丁 | review §4.1、§5.2、§6 | 可避免覆盖用户修改，属于数据保护规则 | 已实施 |
| 6 | P1 | 明确模板的唯一来源和缺失时的处理 | review §4.2 | 当前模板位于仓库根目录，问题主要是路径歧义 | 已实施 |
| 7 | P2 | 增加实际 Obsidian 渲染检查 | review §4.3、§5.1 | 有价值，但当前环境没有稳定的 Obsidian 渲染验证接口 | 暂缓 |
| 8 | P2 | 统一 HANDOFF 文件命名 | review §4.7 | 当前会话已采用具体命名，需固化为规则即可 | 已实施 |
| 9 | P3 | 为论文特定内容增加更多领域化自动分析 | review §8 | 不应把单篇 DeepSeekMath 的内容需求写成通用规则 | 不纳入当前工作流 |

## 本次评估的判断记录

### 已有规则，无需重复修改

- 证据分层已经在 Skill 中区分 `Paper states`、`Inference` 和 `Evaluation`。
- 用户问题和模型解答已经要求放回对应章节，不需要另建一个泛化的问答附录规则。
- 外部输出写入已经要求用户明确保存、检查目录和报告写入结果；本次只补充失败时不得误报。

### 不直接采纳的建议

- “coder initialization 为什么更好”是论文实验现象和机制推断，不是通用工作流缺陷。
- “GRPO 是否普遍优于 PPO”是论文内容评价问题，不应写入通用 Skill。
- “Obsidian 全红是否错误”不能仅由界面颜色判断；在没有稳定渲染器前，不把主题高亮当作自动失败。

## 迭代记录

### 2026-07-27：DeepSeekMath review

- 输入：`SESSION_REVIEW-2026-07-27-DeepSeekMath.md`
- 已实施顺序：1 → 2 → 3 → 4 → 5 → 6 → 8
- 暂缓：7，等待可可靠调用的 Obsidian 或 Markdown 渲染验证环境
- 未纳入：9
- 结果：更新项目规则、paper-reading Skill、README、模板和本优先级文档；没有改写用户的 Obsidian 论文笔记。

## 每次新 review 的处理顺序

1. 读取本文件和全部相关 `SESSION_REVIEW-*.md`。
2. 区分事实、用户评价、模型推断和具体建议。
3. 检查问题是否已经被现有规则覆盖。
4. 评估通用性、严重性、复现条件和实现成本。
5. 将新问题插入合适的优先级位置，不得绕过前面的未完成项。
6. 先更新本文件的判断和状态，再实施工作流变更。
7. 做最小范围修改和验证。
8. 在迭代记录中说明采纳、暂缓或拒绝的理由。
