# 论文阅读项目规范 v0.1.0

## 项目定位

本仓库保存 Codex 论文阅读工作流本身，不保存论文 PDF，也不保存实际阅读笔记。

论文只作为 Codex App 当前对话的临时输入。阅读结束后，整理后的 Markdown 笔记写入本机 Obsidian 知识库，由 `paper-reading` Skill 的本地配置指定输出目录。

## 语言与表达

- 默认使用中文，关键术语保留英文原文。
- 区分“论文明确说明”“根据论文推断”和“批判性评价”。
- 结论优先，避免没有证据支撑的泛泛总结。

## 证据规则

- 关键判断尽量标注 PDF 页码、章节、图、表或附录。
- 不得编造页码、实验结果、数据、引用或作者观点。
- 材料不足时明确写“证据不足”，不要自行补全。
- 不把领域常识写成论文结论。
- 无法读取的公式、图表或补充材料必须明确说明。

## 交互式阅读规则

- 阅读过程默认只保留在当前对话中，不创建论文文件夹，不复制 PDF，不保存中间 Markdown。
- 以论文章节为单位组织交互，把用户问题、模型解答和用户理解变化放回对应章节。
- 只有收到明确的保存指令后，才生成并写入最终 Markdown。
- 保存前必须检查本地输出配置和目标文件是否存在。
- 目标文件已存在时不得覆盖，先询问用户如何处理。

## 工作流反馈

发现反复出现的阅读偏差或保存问题时，优先更新 `paper-reading` Skill 或本文件中的规则。

## SESSION_REVIEW 复盘机制

- 每次论文阅读会话结束后，可在仓库根目录生成 `SESSION_REVIEW-YYYY-MM-DD-论文短名.md`；同一天有多次会话时使用 `-01`、`-02` 等后缀。
- `SESSION_REVIEW` 用于记录沟通问题、阅读重点偏差、证据边界、保存/编码/工具问题和后续建议；它不是 Obsidian 论文笔记，也不是“提到什么就必须修改什么”的变更清单。
- 处理每份复盘前必须完整阅读，并区分事实、诊断、建议、论文特定问题和真正的通用工作流缺陷。
- 是否修改工作流要根据通用性、严重性、复现条件、收益和实现成本判断；不得因为文档提出了建议就盲目修改。
- 所有优化必须按照 `WORKFLOW_OPTIMIZATION_PRIORITY.md` 的优先级顺序推进。实施、暂缓、拒绝或不纳入的项目，都要在优先级文档中记录理由。
- 修改前先保护用户的现有 Obsidian 笔记，优先采用最小范围变更，并在修改后进行相应验证。
- `HANDOFF-论文短名-YYYY-MM-DD.md` 可用于记录会话交接信息，与 `SESSION_REVIEW` 分开管理。
- `SESSION_REVIEW-*` 和 `HANDOFF-*` 默认是本地会话产物，不同步到远程仓库。
